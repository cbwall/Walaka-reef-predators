---
title: "Walaka Impacts on Reef Predators at a Remote Atoll"
author: "CG Meyer, et al. (in review); Code by C. Wall, R. Garcia-Seoane"
date: "2025-09-18"
output:
  html_document:
    code_folding: hide
    toc: yes
    toc_depth: 4
    toc_float: yes
editor_options: 
  chunk_output_Type: console
---
## Project Overview 
Extreme storms can rapidly devastate coral reefs, yet their consequences for apex predators remain unclear. We combined year-long acoustic monitoring of 25 tagged predators (giant trevally *Caranx ignobilis*, Galapagos sharks *Carcharhinus galapagensis*, tiger sharks *Galeocerdo cuvier*) at French Frigate Shoals (Hawaiʻi) with bulk and compound-specific amino-acid stable isotope analyses of pre-storm muscle tissue to assess survival, space use, and trophic pathways surrounding Category-4 Hurricane Walaka (October 2018). 

<center>  
  
![**Image**. Giant trevally (*Caranx ignobilis*) in the Northwestern Hawaiian Islands. *Photo credit*: C. Wall](output/photos/ulua3-Maro-north.CW.jpg){width=80%}
 
</center>  
```{r setup, include=FALSE}
if (!require('knitr')) install.packages('knitr'); library('knitr')
knitr::opts_chunk$set(warning=FALSE, message=FALSE, fig.align='center')
```

```{r packages, echo=FALSE}
#load packages and data
# load packages
if (!require("pacman")) install.packages("pacman") # for rapid install if not in library

# use pacman to load all the packages you are missing!
pacman::p_load("knitr", "ggord", "MASS", "tidyverse", "plyr", "dplyr", "gridExtra", 
               "cowplot", "ggplot2", "R2jags", "MixSIAR", "remotes", 'SIBER', 'ggpubr', 'patchwork', 
               "remotes", "vegan", "pairwiseAdonis")
```

### Data Description
Data here consists of bulk and amino acid δ^13^C and δ^15^N isotope values for Giant trevally (*Caranx ignobilis*) and Galapagos sharks (*Carcharhinus galapagensis*) from the Northwestern Hawaiian Islands, at French Frigate Shoals atoll ('ōlelo Hawai'i: *Lalo* or *Mokupāpapa*). We also include data used to calculate residence index using telemetry, as well as info on tagging dates and consumer fork length. Lastly, we incorporate the data from `Skinner et al. (2021)` reporting δ^13^C isotope values for essential amino acids (EAAs) collected inshore and offshore, using coral reef fishes and invertebrates (plankton and squid) in the Maldives. These are used as training data to assign fish consumers and determine the proportion support of food web support to consumers.  

`Skinner, et al,. 2021. Offshore pelagic subsidies dominate carbon inputs to coral reef predators. Science advances 7.`

### Bulk tissue analysis
Use δ^13^C and δ^15^N biplots and perform `SIBER: Stable Isotope Bayesian Ellipse in R` analysis. This will plot the raw data with the standardized ellispe area, with 40% probability. This calculates the Isotope Niche Width
```{r Siber, fig.show='hide', results='hide'}
### BULK d13C-d15N BIPLOT AND STANDARD ELLIPSES### 

# load the SIBER package: library(SIBER)
# load the data: library(dplyr)

# load bulk data 
All.data<-read.csv("data/Walak_Trevl_Gshark_update.csv")

# subset to be only bulk d13C and d15N

df.bulk<- All.data %>%
  filter(Data.source %in% c('This study')) %>% # only keep data from Walaka fishes
  select(Group, d13C_bulk, d15N_bulk) # reduce to only bulk isotope data

df.bulk<-na.omit(df.bulk) # remove NAs

df.bulk$Group<-factor(df.bulk$Group, levels=c("GT", "GS")) # make factor level and order

# make levels for Siber
df.bulk$group<- ifelse(df.bulk$Group=="GT", "1", "2")
df.bulk$community<- ifelse(df.bulk$Group=="GT", "1", "2")

# rename d13C as "iso1", and d15N as "iso2", remove Group
df.Siber<- df.bulk %>% 
  dplyr::rename(iso1 = d13C_bulk, iso2 = d15N_bulk) %>%
  select(-Group)

# create the siber object
siber <- createSiberObject(df.Siber)

# create lists of plotting arguments to be passed onwards to each of the three plotting functions
community.hulls.args <- list(col = 1, lty = 1, lwd = 1)
group.ellipses.args  <- list(n = 100, p.interval = 0.4, 
                             lty = 2, lwd = 2)
group.hull.args      <- list(lty = 2, col = "grey20")

# ellipses and group.hulls are set to TRUE or T for short to force their plotting
palette(c("dodgerblue3", "orangered"))
par(mfrow=c(1,1))
plotSiberObject(siber,
                ax.pad = 2, 
                hulls = F, community.hulls.args, 
                ellipses = T, group.ellipses.args,
                group.hulls = F, group.hull.args,
                bty = "L",
                iso.order = c(1,2),
                xlab = expression({delta}^13*C~'‰'),
                ylab = expression({delta}^15*N~'‰'),
                x.limits=c(-17.7, -12.7),
                y.limits = c(9.1, 11.5)
)
```

After running SIBER, plot the SIBER biplot with 40% ellipses for the two shark species.  
This is part of **Figure 4**.
```{r Siber plot, fig.cap="**Figure 4a.** Muscle tissue Isotopic biplot of from FFS predators. Isotopic biplot (δ^13^C vs. δ^15^N) of bulk muscle tissue from giant trevally and Galapagos sharks. Dashed ellipses represent 40% Bayesian ellipses.", fig.dim=c(5,5)}
# go with ggplot, bc base R is BASSSSSIC
Siber.plot<-ggplot(data=df.bulk, aes(y=d15N_bulk, x=d13C_bulk))+
  geom_point(aes(fill=Group),  shape=21, size=5)+
  xlab(expression({delta}^13*C~'‰'))+
  ylab(expression({delta}^15*N~'‰'))+
  xlim(-17.7, -12.7)+
  ylim(9.3, 11.5)+
  theme_bw() + theme(panel.grid.major = element_blank(), panel.grid.minor = element_blank())+
  scale_fill_manual(values=c("dodgerblue3", "orangered"),
                    name = "", 
                    labels = c("Giant trevally (GT)", "Galapagos shark (GS)"))+
  guides(fill = guide_legend(position="inside")) + 
  theme(legend.background=element_blank()) +
  theme(legend.position.inside = c(0.81, 0.95)) +
# add in the ellipses from Siber
  stat_ellipse(aes(group = (Group),
                   color = Group), 
               alpha = 0, 
               level = 0.4,
               lty=2, linewidth=0.9,
               type = "norm",
               geom = "polygon") + 
  scale_color_manual(values=c("dodgerblue3", "orangered")) +
  guides(color="none")

Siber.plot
```

Calculate the posterior estimates of the `Bayesian Standard Ellipse Area (SEA~B~)`. We will add this to the SIBER biplot generated above. The SEA~B~ plot shows the  95%, 75%, and 50% credible intervals for each species, with modes indicated by black circles.
```{r SEAb, results='hide'}
# calculate summary statistics for each group: TA, SEA and SEAc
group.ML <- groupMetricsML(siber)
print(group.ML)

# use Bayesian Inference to calculate uncertainty around ellipses
# options for running jags
parms <- list()
parms$n.iter <- 2 * 10^4   # number of iterations to run the model for
parms$n.burnin <- 1 * 10^3 # discard the first set of values
parms$n.thin <- 10     # thin the posterior by this many
parms$n.chains <- 2        # run this many chains

# define the priors
priors <- list()
priors$R <- 1 * diag(2)
priors$k <- 2
priors$tau.mu <- 1.0E-3

# fit the ellipses which uses an Inverse Wishart prior on the covariance matrix Sigma, and a vague normal prior on the means
# fitting is via the JAGS method
ellipses.posterior <- siberMVN(siber, parms, priors)

# the posterior estimates of the ellipses for each group can be used to calculate the SEA.B for each group.
SEA.B <- siberEllipses(ellipses.posterior)
```

```{r SEAb plot, results='hide', fig.cap="**Figure 4b.** Posterior estimates of the Bayesian Standard Ellipse Area (SEA~B~).", fig.dim=c(4,4)}
# colors for SEAb plot
SEA.col<-matrix(c("gray85", "skyblue1", "dodgerblue3","skyblue1","gray85",
                  "gray85","coral", "orangered","coral","gray85"), ncol=2, nrow=5)

par(mar=c(2,4.5,0,0))

### plot the SEAb figure                  
siberDensityPlot(SEA.B, 
                 xticklabels= c("GT", "GS"), 
                 xlab = "",
                 ylab = expression(paste("SEA " ('‰' ^2))),
                 bty = "L",
                 las = 1,
                 clr = SEA.col,
                 #main = "SIBER ellipses on each group",
                 ylim = c(0, 4))

# will need this to export, but Rmd will run with the figure above
SEAb.plot<- recordPlot() # save SEAb plot in list
#plot.new() # close the "recording" of the plot -- really difficult to save this within R.
#SEAb.plot
```

```{r combine Fig 4, fig.show='hide', results='hide'}
# add to other plot. Will require cleaning up in Affinity. SIBER isn't most user friendly.
  ggdraw() +
  draw_plot(Siber.plot) +
  draw_plot(SEAb.plot, .17, .712, .34, .22)

dev.copy(pdf, "figures/Fig.4.Siber.pdf", height=8, width=8)
dev.off()
```

```{r Siber add ons, eval=FALSE, include=FALSE}
########### other useful stats
# add red x's for the ML estimated SEA-c
#points(1:ncol(SEA.B), group.ML[3,], col="red", pch = "x", lwd = 2)

# calculate some credible intervals 
cr.p <- c(0.95, 0.99) # vector of quantiles

# call to hdrcde:hdr using lapply()
SEA.B.credibles <- lapply(
  as.data.frame(SEA.B), 
  function(x,...){tmp<-hdrcde::hdr(x)$hdr},
  prob = cr.p)

print(SEA.B.credibles)

# do similar to get the modes, taking care to pick up multimodal posterior distributions if present
SEA.B.modes <- lapply(
  as.data.frame(SEA.B), 
  function(x,...){tmp<-hdrcde::hdr(x)$mode},
  prob = cr.p, all.modes=T)

print(SEA.B.modes)
```

### δ^15^N Amino Acid Analysis
Amino acid (AA) nitrogen isotope analysis δ^15^N is uses Source-AAs (notably Glutamic Acid [Glx] that are integrated into consumers and reflect the base of the food web. These internal baselines are combined with Trophic-AA (notably Phenylalanine [Phe]) analysis to obtain better resolution into trophic positions (TP) of consumers. In areas were protists are important in food webs, there can be a "missing step" in trophic position (so called, "Protistan Trophic Position"). This is resolved by substituting Alanine (Ala) for Glx in TP calculations.  

`Y. Chikaraishi, et al. Limnol Oceanogr Methods 7, 740–750 (2009)`. 
`M. Décima, et al. Limnol Oceanogr 62, 2308–2322 (2017)`.  
  
#### Calculations

**Equation 1 and 2.** *Trophic position of fishes using AA-Glx and Phe, with calculations based on two approaches.
$${TP_{Glx-Phe}} = \frac{(δ^{15}N_{Glx}~ - δ^{15}N_{Phe} - \beta)}{TDF} +1$$
 
In this equation, *β* is the difference between δ~15~N values between Glx and Phe in marine primary producers, while *TDF* denotes the trophic discrimination factor, representing the ~15~N enrichment between Glx and Phe per trophic level. 

- We adopt `β = 3.1‰` (Garcia-Seoane et al. 2023)     
- We also report two TDF in `TP~Glx-Phe~` using...   
- `TDF = 5.7 ± 0.3‰ for trevally` (Bradley et al. 2015)
- `TDF = 5.9 ± 0.1‰ for Galapagos sharks`, originally developed for sharks and rays (Dale et al. 2011; Papastamatiou et al. 2011).  
  

**Equation 3.**  *Trophic position  of fishes using AA-Ala and Phe, with calculations following `Decima et al (2017)`.
 $${TP_{Ala-Phe}} = \frac{(δ^{15}N_{Ala}~ - δ^{15}N_{Phe} - \beta)}{TDF} +1$$
   
We also calculated TP using δ~15~N values of Ala and Phe (TP~Ala-Phe~), which account for the contribution of protists.
- `β = 3.2 ± 1.2‰`, representing the δ~15~N difference between Ala and Phe in marine primary producers
- `TDF = 4.5 ± 2.1‰‰`, representing the ~15~N enrichment between Ala and Phe per trophic level for both trevally and sharks. (Décima et al. 2017; Décima and Landry 2020; Shea et al. 2023) 
  
**Equation 4.** *Protistan Trophic Steps.*
$$PTS = (δ^{15}N_{Ala-Phe}~ - δ^{15}N_{Glx-Phe})$$

- Protistan trophic steps (i.e.,PTS) recorded in trevally and sharks was estimated using the difference between TP~Ala-Phe~ and TP~Glx-Phe~.

**Equation 5.** *Average source amino acid δ~15~N.*
$$δ^{15}N_{SAA} = mean(δ^{15}N_{Phe}~ + δ^{15}N_{Lys})$$
  
- To assess changes in nitrogen sources at the base of the food web, we calculated the average δ^15^N values of the source AAs, Phe and Lys, collectively referred to as δ^15^N~SAA~ 


```{r df smithing}
# info on calculations


### BOXPLOTS, ANOVA AND CORRELATIONS WITH RESIDENCY_INDEX###

# load ggplot2 package: library(ggplot2)
# load the data: library(dplyr)
AA.indic <- All.data %>%
  dplyr::select(Group, Residency, RI_corrected, RI_pre_storm, RI_post_storm_total, RI_post_4wk, RI_post_3mo,
                d15N_SAA, TPGlx_Phe, TPAla_Phe, PTS)

AA.indic<-na.omit(AA.indic)

AA.indic$Residency<-factor(AA.indic$Residency, levels=c("low", "high"))

AA.indic$Res.cat<-ifelse(AA.indic$Residency=="low", "< 0.1 offshore", ">= 0.1 inshore")
AA.indic$Res.cat<- factor(AA.indic$Res.cat, levels=c("< 0.1 offshore", ">= 0.1 inshore"))

AA.indic$Group<-factor(AA.indic$Group, levels=c("GT", "GS"))
```

#### Residency Histogram
Plot a histogram of residency. This will be used to motivate the binning as "inshore and offshore."

- We chose to bin as <0.1 as inshore and >- 0.1 as offshore.
- This is **Figure S3**.
```{r histogram, fig.cap="**Figure S3.** Histogram showing the frequency distribution of Residency Index values for giant trevally and Galapagos shark sampled for stable isotope analysis.", fig.dim=c(5,5), results='hide'}

hist(AA.indic$RI_corrected, main="", ylab="Number of Individuals", xlab="Residency Index", 
     breaks = seq(0,0.7, 0.035))
box(lty = 1, col = 'black')

dev.print(pdf, "figures/Fig.S3.histogram.pdf", width=6, height=6)
dev.off()
```

Run for loops to create boxplots of δ^15^N amino acid metrics with fish residency. Here we use two categories for residency based on residency scores.  

- **low inshore residency** = scores of < 0.1, classified as "offshore". 
- **high inshore residency** = scores >=0.1, classified as "inshore"

```{r boxplot and regression for residency, results='hide', fig.show='hide'}
#### #### #### #### #### #### #### 
#### boxplots high residency 
# (Residency_index ≥ 0.1, ∼inshore) vs low residency (Residency_index < 0.1, ∼offshore) 
# list of variables to analyze
vars <- c("d15N_SAA", "TPGlx_Phe", "TPAla_Phe", "PTS") # we use these for boxplot and for regression
p.box<- list()

# data frame to store model results
results.box <- data.frame()

for (var in vars) {
  # fit linear model
model <- lm(AA.indic[[var]] ~ AA.indic$Res.cat)
summary_model <- summary(model)
  
# save results in the results data frame
results.box <- rbind(results.box, data.frame(
    Variable = var,
    Intercept = coef(model)[1],
    Slope = coef(model)[2],
    R2 = summary_model$r.squared,
    p_value = summary_model$coefficients[2,4]
  ))

# plot using ggplot2
  p.box[[var]] <- ggplot(AA.indic, aes(x = Res.cat, y = .data[[var]])) +
    geom_boxplot(aes(fill = Res.cat), alpha=0.8) +
    xlab("Residency")+
    scale_fill_manual(values=c("lightblue3", "springgreen4"))+
    scale_y_continuous(labels = scales::number_format(accuracy = 0.1))+
    theme_bw() + theme(panel.grid.major = element_blank(), panel.grid.minor = element_blank()) +
    guides(fill="none")
  
  #print(p.reg)
  #ggsave(p.reg, file=paste0("figures/plot_", var,".pdf"), width = 5, height = 5, units = "in")
}

wrap_plots(p.box, ncol=1) # plot list with 'patchwork'
#ggsave("figures/FigSx.boxplot.Lpanel.pdf", height=12, width=3.5) # save and export as a pdf
#dev.off()

# of thge above models, only these two are significant ANOVA
model.SAA <- aov(d15N_SAA ~ Residency, data = AA.indic)
anova(model.SAA) # signif p=0.016

model.TPgp <- aov(TPGlx_Phe ~ Residency, data = AA.indic)
anova(model.TPgp) # signif p=0.026
```

Results from categorical tests of "inshore vs offshore" effects on 
```{r boxplot models}
# display results for boxplot tests
print(results.box) #
```

Use linear regressions to test correlations between δ^15^N amino acid metrics and fish Residency Index.
```{r regression for residency, results='hide', fig.show='hide'}
#######  Regression plots: correlations with Residency_index  ####### 
# set up for loop 

# data frame to store results
results <- data.frame()

# linear regression and plotting for each variable
p.reg = list() # a list to save plots in

###### run the loop
for (var in vars) {
# fit linear model
model <- lm(AA.indic[[var]] ~ AA.indic$RI_corrected)
summary_model <- summary(model)
  
# save results in the results data frame
results <- rbind(results, data.frame(
    Variable = var,
    Intercept = coef(model)[1],
    Slope = coef(model)[2],
    R2 = summary_model$r.squared,
    p_value = summary_model$coefficients[2,4]
  ))
  
# plot using ggplot2
  p.reg[[var]] <- ggplot(AA.indic, aes(x = RI_corrected, y = .data[[var]])) +
    geom_point(aes(shape = Group), color="springgreen4", size = 2) +
    scale_shape_manual(values=c(19,17))+
    geom_smooth(method = "lm", se = TRUE, color = "springgreen4", fill = "springgreen4", alpha=0.2) +
    labs(x = "Residency index",
         y = var)+
    theme(aspect.ratio = 1) +
    scale_y_continuous(labels = scales::number_format(accuracy = 0.1))+
    theme_bw() + theme(panel.grid.major = element_blank(), panel.grid.minor = element_blank())
  
  #print(p.reg)
  #ggsave(p.reg, file=paste0("figures/plot_", var,".pdf"), width = 5, height = 5, units = "in")
}

# view and save plots
# wrap_plots(p.reg, ncol=1) # plot list with 'patchwork'

######## combine plots
box<-grid.arrange(grobs= p.box, ncol=1) # save as object that isn't a 'grob'
reg<-grid.arrange(grobs= p.reg, ncol=1)
```

Results from linear regressions of δ^15^N amino acid metrics and Residency Index.
```{r residence and d15N output}
# display results for regressions
print(results) # significant for d15N-SAA (p=0.041)
```

Combine boxplots and regression plots for δ^15^N amino acid data with residency index to produce 8 panel figure. This is compiled **Figure S4**.
```{r combined S4 plot, results='hide', fig.cap="**Figure S4.** (*Left*) Boxplots showing differences in δ^15^N~SAA~, TP~Glx-Phe~, TP~Ala-Phe~, and PTS between fish with low residency index (< 0.1, greater offshore nutrient contribution) and a high residency index (≥ 0.1, greater inshore nutrient contribution). (*Right*) Panels show the relationships between predators (giant trevally [circles], Galapagos shark [triangles]) and δ^15^N~SAA~, TP~Glx-Phe~, TP~Ala-Phe~, and PTS.", fig.dim=c(6,10)}

plot_grid(box, reg, ncol=2, rel_widths=c(0.5, 1)) # combine and set widths
ggsave("figures/Fig.S4.box.regr.rev.pdf", height=12, width=7) # save and export as a pdf
dev.off()
```

### δ^13^C Amino Acid Analysis
Here we use the δ^13^C values of essential amino acids to trace the determine the reliance of fishes on inshore vs. offshore production. 
```{r load data, echo=FALSE}
# load data or Carbon EAAs
df<-read.csv("data/Walak_Trevl_Gshark_update.csv")

# simplify nmes
df<- df %>%
  dplyr::rename(Thr = d13CThr,
         Val = d13CVal,
         Leu = d13CLeu, 
         Phe = d13CPhe, 
         Lys = d13CLys,
         Thrn = d13CThr.norm,
         Valn = d13CVal.norm,
         Leun = d13CLeu.norm, 
         Phen = d13CPhe.norm, 
         Lysn = d13CLys.norm)

# parse data
raw.df<- df %>% dplyr::select(SampleID_new, Group, Thr, Val, Leu, Phe, Lys)
raw.df<-na.omit(raw.df)

norm.df<- df %>% dplyr::select(SampleID_new, Group, Thrn, Valn, Leun, Phen, Lysn)
norm.df<-na.omit(norm.df)
```

#### Linear Discriminant Analysis
Subset the data and use 'sources' to generate training data that will later be used to assign consumer. It is important that sources are distinct and have high accuracy in their assignment, otherwise they may not be informative as distinct energy channels/prey items.   
  
Source groups consisted of coral reef consumers, including fishes, plankton, and squid, which reflect the primary producers of their respective habitats (benthic versus pelagic) and food webs (inshore versus offshore). Three source groups were used: “reef-associated plankton” represented by diurnal (*Caesio varilineata*, *Caesio xanthonota*) and nocturnal fishes (*Myripristis violacea*); “pelagic plankton” derived from squid (*Uroteuthis duvaucelii*); and “coral” derived from coralivorous fish (*Chaetodo meyeri*) found in `Skinner et al. (2021).`

```{r LDA}
########## LDA setup

raw.df$SampleID_new<-as.factor(raw.df$SampleID_new)
raw.df$Group<-as.factor(raw.df$Group)

# remove all samples, keep source
prod <- raw.df[!(raw.df$Group == "GT" |raw.df$Group == "GS"),]
prod$Group<-droplevels(prod$Group) # drop sample levels

# Run an LDA with a jackknifing model fit to look at error rate
# 'CV = TRUE' makes the LDA run a jackknifed - leave one out - model fit
All.lda <- lda(Group ~Thr + Val + Leu + Phe + Lys, data = prod, CV = TRUE)
```

Here, show LDA classification of sources performed with 100% success.
```{r LDA performance}
#Create a table which compares the classification from the LDA model to the actual classification
All.reclass <- table(prod$Group, All.lda$class) #100% success

#Total percent of samples correctly classified is the sum of the diagonal of this table
sum.dig<-sum(diag(prop.table(All.reclass))) #100% !

accur<-mean(All.lda$class == prod$Group) # mean accuracy

#Percent of each producer Group correctly reclassified: 100% for all groups
reclass<-diag(prop.table(All.reclass, 1))

# print results
All.reclass
```

Show the training data with the coefficients of linear discriminants and proportion of trace for LD1 and LD2. This shows the EAA that are driving patterns across the data visualization, as well as the % trace explained on each axis. Here 79% of variation is explained by PC1 and this is strongly affected by threonine (Thr) and phenylalanine (Phe).  

```{r LDA output, fig.show='hide'}
#Create a training LDA function from the library data
All.train <- lda(Group~ Thr + Val + Leu + Phe + Lys, data = prod)
All.train

# save as txt file
sink("output/LDA output/Walak_LDA_Train_Info.txt")
print(All.train)
sink()
```

```{r LDA base plot, fig.show='hide', results='hide', echo=FALSE}
# plot LD1 and LD2 to show the sources
prod$Group <-factor(prod$Group, levels=c("PelagicPlankton", "ReefPlankton", "Coral"))

Source.LDA <- ggord(All.train, prod$Group, arrow=0,
                    txt = NULL, veclsz = 0, vectyp="blank", # remove vectors and arrows
                    cols = c("lightblue3", "olivedrab4","gold3"), grp_title = "Producers",
                    ellipse_pro = 0.95, xlim = c(-10,14), ylim = c(-8,6), alpha=0.8) + theme_classic() +
  theme(legend.position = 'top')

# view plot, not used in manuscript
Source.LDA
#dev.print(pdf, "figures/Fig.NA.Walak_LDA.pdf", height=6, width=8)
#dev.off()

# Write a csv file for the coefficients of LD1/2
lda.info <- as.data.frame(All.train$scaling)
write.csv(lda.info, "output/LDA output/Walak_LDA_loadings.csv", row.names=FALSE)

#Create a data frame with these LDA coordinates
AllProdPredict <- data.frame(SampleID_new = prod$SampleID_new, Group = prod$Group, LD.class = "Source", predict(All.train)$x)

#Write a csv file for the LDA coordinates
write.csv(AllProdPredict, "output/LDA output/Walak_LDA_sorc_coords.csv", row.names = FALSE)
```

Classify consumers (giant trevally or Galapagos shark) as belonging to the three source groups. 

- *We show* the classification gives about 1/2 of the trevally as inshore vs. offshore, with 3 of 4 sharks being inshore.
```{r LDA classify consumers}
####### ######## ######## ######## #####
######## Classify Trevaly and Sharks ####
######## ######## ######## ######## #####

# remove all samples, keep source
cons.df <- raw.df[(raw.df$Group == "GT" |raw.df$Group == "GS"),]
cons.df$Group<-droplevels(cons.df$Group) # drop sample levels

cons.predict <- predict(object = All.train, newdata = cons.df)

cons.predict.data <- data.frame(SampleID_new = cons.df$SampleID_new, Group = cons.df$Group, 
                                LD.class = cons.predict$class, cons.predict$x)

All.reclass <- table(cons.df$Group, cons.predict$class) 
All.reclass
#  GS: 1 as PelagicPlank, 3 as ReefPlankton
#  GT: 7 as PelagicPlank, 6 as ReefPlankton
```

```{r stich dfs, echo=FALSE}
###### ###### write and compile data outputs from LDA
# write a csv file for the consumer LDA info
write.csv(cons.predict.data, "output/LDA output/Walak_LDA_cons_pred.csv", row.names=FALSE)

# combine the consumer data and source LDA #
LDA.df<-rbind(cons.predict.data, AllProdPredict)

# combine with raw and mean-normalized data and rearrange
raw.norm.LD.data <- merge(df, LDA.df, by = "SampleID_new", all.x = TRUE)

# remove extra column and rename the other
raw.norm.LD.data<- raw.norm.LD.data %>% 
  dplyr::select(-Group.y)
raw.norm.LD.data <- raw.norm.LD.data %>% 
  dplyr::rename("Group" = "Group.x")

###### ###### ######
# export all data
write.csv(raw.norm.LD.data, "output/Walak_LDA.data.compiled.csv")
```

Plot the LDA with producer groups and the consumers. **This is Figure 5**
```{r LDA figure, fig.cap="**Figure 5.** Linear discriminate analysis (LDA) of δ^13^C~EAA~ fingerprints from reef-associated plankton, pelagic plankton and coral compared with those of predators. Ellipses denote 95% confidence intervals for each source group.", results='hide'}
#### plot it
raw.norm.LD.data$Group<-factor(raw.norm.LD.data$Group, 
                      levels=c("PelagicPlankton", "ReefPlankton", "Coral", 
                               "GT", "GS"))

lda.5.colors <- c("lightblue3", "olivedrab4","gold3", "dodgerblue3", "orangered")
lda.5.lines<-c("lightblue4", "darkgreen","goldenrod", "blue", "red")

LDA.scat.Group <- ggplot(raw.norm.LD.data, aes(x = LD1, y = LD2))+
  geom_point(size = 4.5, color="black",
             aes(fill = Group, color = Group, shape = Group), alpha = 0.9) +
  stat_ellipse(type = "norm", level = 0.95, linewidth = 0.5, aes(lty = Group, color = Group))+
  scale_color_manual(values = lda.5.lines)+
  scale_fill_manual(values = lda.5.colors)+
  scale_shape_manual(values = c(24, 24, 24, 21, 21))+
  scale_linetype_manual(values = c(2,2,2,0,0))+
  xlab("LD1 (79.1%)") + ylab("LD2 (20.9%)") +
  ggtitle(expression(paste("LDA: Measured-", delta^{13}, C[EAA]))) +
  theme_classic() + guides(lty = "none")

LDA.scat.Group
dev.copy(pdf, "figures/Fig.5.Walak_LDA_sorc_consumer_rev.pdf", width=6.5, height=6)
dev.off()
```

Check if normalization impacts the outcome of analysis. This "mean-centered" LDA is **Figure S6**.
```{r LDA norm, results='hide', echo=FALSE}
##### Gut Check ##### 

# Check if normalization impacts the outcome of analysis
# Here us normalized EAAs instead of (raw) measured EAAs
# TLDR: no difference in assignment, some differences in ordination (LD1 >>>) and Coral looking more like Reef Plankton

########## LDA setup NORMALIZED
norm.df$SampleID_new<-as.factor(norm.df$SampleID_new)
norm.df$Group<-as.factor(norm.df$Group)

# remove all samples, keep source
prod.norm <- norm.df[!(norm.df$Group == "GT" |norm.df$Group == "GS"),]
prod.norm$Group<-droplevels(prod.norm$Group) # drop sample levels

#Run an LDA with a jackknifing model fit to look at error rate
#'CV = TRUE' makes the LDA run a jackknifed - leave one out - model fit
All.lda.norm <- lda(Group ~Thrn + Valn + Leun + Phen + Lysn, data = prod.norm, CV = TRUE)


#Create a table which compares the classification from the LDA model to the actual classification
All.reclass.norm <- table(prod.norm$Group, All.lda.norm$class) 

#Total percent of samples correctly classified is the sum of the diagonal of this table
sum.dig.norm<-sum(diag(prop.table(All.reclass.norm))) #90.4%

#Percent of each producer Group correctly reclassified: 91% for all groups
reclass.norm<-diag(prop.table(All.reclass.norm, 1)) # (78% coral, 100% plankton, 92% reef)

# print results: 2 reef plankton as coral
All.reclass.norm

#Create a training LDA function from the library data
All.train.norm <- lda(Group~ Thrn + Valn + Leun + Phen + Lysn, data = prod.norm)
All.train.norm

# plot LD1 and LD2 to show the sources: NORMALIZED
prod.norm$Group <-factor(prod.norm$Group, levels=c("PelagicPlankton", "ReefPlankton", "Coral"))

Source.LDA.norm <- ggord(All.train.norm, prod.norm$Group, arrow=0,
                    txt = NULL, veclsz = 0, vectyp="blank", # remove vectors and arrows
                    cols = c("lightblue3", "olivedrab4","gold3"), grp_title = "Producers",
                    ellipse_pro = 0.99, xlim = c(-14,10), ylim = c(-8,6), alpha=0.5) + 
  theme_classic() + theme(legend.position = 'top')


#Create a data frame with these LDA coordinates NORMALIZED
AllProdPredict.norm <- data.frame(SampleID_new = prod.norm$SampleID_new, Group = prod.norm$Group, 
                                  LD.class = "Source", predict(All.train.norm)$x)


######## Classify Trevaly and Sharks #### NORMALIZED
######## ######## ######## ######## #####

# remove all samples, keep source
cons.df.norm <- norm.df[(norm.df$Group == "GT" | norm.df$Group == "GS"),]
cons.df.norm$Group<-droplevels(cons.df.norm$Group) # drop sample levels

cons.predict.norm <- predict(object = All.train.norm, newdata = cons.df.norm)

cons.predict.data.norm <- data.frame(SampleID_new = cons.df.norm$SampleID_new, Group = cons.df.norm$Group, 
                                LD.class = cons.predict.norm$class, cons.predict.norm$x)

All.reclass.norm <- table(cons.df.norm$Group, cons.predict.norm$class) 
All.reclass.norm
#  GS: 1 as PelagicPlank, 3 as ReefPlankton
#  GT: 7 as PelagicPlank, 6 as ReefPlankton

############## stitch

# combine the consumer data and source LDA #
LDA.df.norm<-rbind(cons.predict.data.norm, AllProdPredict.norm)

# combine with raw and mean-normalized data and rearrange
LD.data.norm <- merge(df, LDA.df.norm, by = "SampleID_new", all.x = TRUE)

# remove extra column and rename the other
LD.data.norm<- LD.data.norm %>% 
  dplyr::select(-Group.y)
LD.data.norm <- LD.data.norm %>% 
  dplyr::rename("Group" = "Group.x")

############# plot it
LD.data.norm$Group<-factor(LD.data.norm$Group, 
                      levels=c("PelagicPlankton", "ReefPlankton", "Coral", 
                               "GT", "GS"))

LD.data.norm.group <- ggplot(LD.data.norm, aes(x = LD1, y = LD2))+
  geom_point(size = 4.5, color="black",
             aes(fill = Group, color = Group, shape = Group), alpha = 0.9) +
  stat_ellipse(type = "norm", level = 0.95, linewidth = 0.5, aes(lty = Group, color = Group))+
  scale_color_manual(values = lda.5.lines)+
  scale_fill_manual(values = lda.5.colors)+
  scale_shape_manual(values = c(24, 24, 24, 21, 21))+
  scale_linetype_manual(values = c(2,2,2,0,0))+
  xlab("LD1 (96.5%)") + ylab("LD2 (3.5%)") +
  ggtitle(expression(paste("LDA: Normalized ", delta^{13}, C[EAA]))) +
  theme_classic() + guides(lty = "none")

LD.data.norm.group
dev.copy(pdf, "figures/Fig.S6.LDAnorm_sorc_consumer_rev.pdf", width=6.5, height=6)
dev.off()
```

### EAA database
Use fish proxies from studies across ocean provinces to test how well these guilds align. We use data from fishes and squid collected in...
  
- Central Pacific/Kiribati: `Ramirez et al. 2025`
- Red Sea: `McMahon et al. 2016`
- Indian Ocean: `Skinner et al. 2021`
  
We retain only fishes groups classified as corallivores and (in-shore and offshore) planktivores.  
**This is Figure S5.**

```{r database test}
# load data
df.lit<-read.csv("data/Endmember library.csv")
df.lit<-df.lit[!df.lit$site %in% c("oceanic", "Oceanic"),]  # remove Kelton's ocean fishes, simialr to nearshore but some differences exist

# parse data
raw.df.lit<- df.lit %>% 
  dplyr::select(source, ocean, location, site, sample.type, group, group.2, species.taxa, 
                Thr13C, Val13C, Phe13C, Leu13C)

# first, set the factor levels and make factors for columns
make.fac <- c("source", "location", "site", "ocean", "group", "group.2")
raw.df.lit[make.fac] <- lapply(raw.df.lit[make.fac], factor)
raw.df.lit$Val13C<-as.numeric(raw.df.lit$Val)

# simplify nmes
raw.df.lit<- raw.df.lit %>%
  dplyr::rename(Thr = Thr13C,
                Val = Val13C,
                Phe = Phe13C, 
                Leu = Leu13C) 

raw.df.lit <- raw.df.lit %>% dplyr::filter(!(Val %in% c(NA)))     # remove and NAs for 

# remove unwanted groups for now
raw.df.lit<-raw.df.lit[!raw.df.lit$group.2 %in% c("coral.host", "coral.symbiont"),]
raw.df.lit$group.2<-droplevels(raw.df.lit$group.2) # drop sample levels

## mean center data
# make an ID column and run the mean normalization procedure
raw.df.lit$SampleID_new <- 1:nrow(raw.df.lit)
for(i in 1:length(raw.df.lit$SampleID_new)){
  raw.df.lit$Thr.n[i] <- (raw.df.lit$Thr[i]-mean(as.numeric(raw.df.lit[i,9:12])))
  raw.df.lit$Val.n[i] <- (raw.df.lit$Val[i]-mean(as.numeric(raw.df.lit[i,9:12])))
  raw.df.lit$Phe.n[i] <- (raw.df.lit$Phe[i]-mean(as.numeric(raw.df.lit[i,9:12])))
  raw.df.lit$Leu.n[i] <- (raw.df.lit$Leu[i]-mean(as.numeric(raw.df.lit[i,9:12])))
}

raw.norm.df.lit<-raw.df.lit

##########################################
##########################################
# just the fishes
# just consumers, but no detritivores
df.cull<-raw.norm.df.lit[!(raw.norm.df.lit$source=="Meyer et al. in review"),]

fish.df<-df.cull[df.cull$group.2 %in% c("corallivore", "reef.zooplanktivore", "pelagic.zooplanktivore"),]

# remove all samples, keep source
fish.df$group.2 <-factor(fish.df$group.2, levels=c("corallivore",
                                             "reef.zooplanktivore", "pelagic.zooplanktivore"))
fish.df$group.2<-droplevels(fish.df$group.2) # drop sample levels
fish.df$source<-droplevels(fish.df$source) # drop sample levels

###### remove outliers
fish.outliers <- fish.df %>% dplyr::filter((SampleID_new %in% c(28, 287, 297, 298, 500, 435, 436)))
fish <- fish.df %>% dplyr::filter(!(SampleID_new %in% c(28, 287, 297, 298, 500, 435, 436)))
write.csv(fish, "output/Lit.fish.training.csv")  

#### simplified dataframes
fish.raw.df<- fish %>% select(source, ocean, sample.type, group, group.2, species.taxa, 
                              Thr, Val, Phe, Leu, SampleID_new)
fish.norm.df <- fish %>% select(source, ocean, sample.type, group, group.2, species.taxa, 
                              Thr.n, Val.n, Phe.n, Leu.n, SampleID_new)


##
fish.lda <- lda(group.2 ~Thr + Val + Phe + Leu, data = fish.raw.df, CV = TRUE)

#Create a table which compares the classification from the LDA model to the actual classification
All.reclass <- table(fish$group.2, fish.lda$class) 
sum(diag(prop.table(All.reclass))) #96%
mean(fish.lda$class == fish$group.2) # mean accuracy

#Percent of each fishucer Group correctly reclassified: 88% for all groups
diag(prop.table(All.reclass, 1)) #96% corallivore, 98% reef zooplankton, 88% pelagic zooplanktivore

#Create a training LDA function from the library data
#Note - you can't use the 'All.lda' object above because the 'CV = TRUE' command was used to create it, and for some reason this won't work with the predict() function
All.train <- lda(group.2~Thr + Val + Phe + Leu, data = fish.raw.df) #Thr + Val + Phe + Leu
All.train

lda.colors<- c("lightblue3", "olivedrab4", "goldenrod")

fish.raw.df$group.2<-factor(fish.raw.df$group.2, levels=c("pelagic.zooplanktivore", "reef.zooplanktivore", "corallivore"))

# plot LD1 and LD2 to show the sources
Fish.LDA.Skin.McMa.Ram <- ggord(All.train, fish.raw.df$group.2, arrow=0,
                    txt = NULL, veclsz = 0, vectyp="blank", # remove vectors and arrows
                    cols = lda.colors,
                    grp_title = "fish consumers",
                    ellipse_pro = 0.95, xlim = c(-8,8), ylim = c(-6,6), alpha=0.6) + theme_classic() +
  theme(legend.position = 'top')

Fish.LDA.Skin.McMa.Ram
#dev.print(pdf, "figures/Fish.LDA.mass.pdf", height=7, width=8)
#dev.off()

#Create a data frame with these LDA coordinates
AllfishPredict <- data.frame(SampleID_new = fish.raw.df$SampleID_new, 
                             species.taxa = fish.raw.df$species.taxa, 
                             group.2 = fish.raw.df$group.2, 
                             LD.class = "Source", 
                             predict(All.train)$x)



#######################
# run PERMANOVA

#factor to test
fish.fac<- fish.raw.df %>% dplyr::select(group.2)
fish.EAA.raw<- fish.raw.df %>% dplyr::select(Thr, Val, Phe, Leu)
fish.EAA.norm<- fish.norm.df %>% dplyr::select(Thr.n, Val.n, Phe.n, Leu.n)

##### raw data
set.seed(51)
EAA.perm.fish<-adonis2(fish.EAA.raw ~ group.2, data = fish.fac, 
                         permutations = 999, method = "euclidian", by = "terms")
EAA.perm.fish # Groups: different at p=0.001, R2=0.48

# run pairwise
set.seed(138)
pairwise.adonis(fish.EAA.raw, factors = fish.raw.df$group.2, sim.method = "euclidian", p.adjust.m = "bonferroni")
# all groups differ from each other


############ normalized data (less good in R2 but same result)
set.seed(151)
EAA.perm.fish.norm<-adonis2(fish.EAA.norm ~ group.2, data = fish.fac, 
                         permutations = 999, method = "euclidian", by = "terms")
EAA.perm.fish.norm # Groups: different at p=0.001, R2=0.20

# run pairwise
set.seed(38)
pairwise.adonis(fish.EAA.norm, factors = fish.norm.df$group.2, sim.method = "euclidian", p.adjust.m = "bonferroni")
# all groups differ from each other


######## ######## ######## ######## #####
######## Classify Walaka predators  ####
######## ######## ######## ######## #####
# remove all samples, keep source
Walak.df<-raw.norm.df.lit[(raw.norm.df.lit$source=="Meyer et al. in review"),]

#only raw data
Walak.df.raw<- Walak.df %>% select(source, ocean, sample.type, group, group.2, species.taxa, 
                              Thr, Val, Phe, Leu, SampleID_new)

Walak.df.raw$group.2<-droplevels(Walak.df.raw$group.2) # drop sample levels

Walak.df.predict <- predict(object = All.train, newdata = Walak.df.raw)

Walak.df.predict.data <- data.frame(SampleID_new = Walak.df.raw$SampleID_new,
                                    group.2 = Walak.df.raw$group.2, 
                                    species.taxa = Walak.df.raw$species.taxa, 
                                    LD.class = Walak.df.predict$class, 
                                    Walak.df.predict$x)

All.reclass <- table(Walak.df.raw$species.taxa, Walak.df.predict$class) 
# GTrev: 9 inshore/zoopl, 4 off shore (pelagic), 
# GShark: 4 inshore/zoopl


# combine the consumer data and source LDA #
LDA.df<-rbind(Walak.df.predict.data, AllfishPredict)

LDA.pool<- rbind(fish, Walak.df)

# combine with raw and mean-normalized data and rearrange
LD.data <- merge(LDA.pool, LDA.df, by = "SampleID_new", all.x = TRUE)

# remove extra column and rename the other
LD.data<- LD.data %>% 
  dplyr::select(-group.2.y, -species.taxa.y)
LD.data <- LD.data %>% 
  dplyr::rename("group.2" = "group.2.x",
                "species.taxa" = "species.taxa.x")

LD.data$group.2<-droplevels(LD.data$group.2)
LD.data$group.2<-factor(LD.data$group.2, levels=c("pelagic.zooplanktivore", "reef.zooplanktivore", "corallivore", "reef.predator", "herbivore"))

write.csv(LD.data, "output/Lit.fish.training_LDAcomb.csv")

########## Figure

lda.3.colors<- c("lightblue3", "olivedrab4", "gold3")
lda.3.lines<-c("lightblue4", "darkgreen","goldenrod")

LD.data.source<-LD.data[!(LD.data$group.2=="reef.predator"),]
LD.data.source$group.2<-droplevels(LD.data.source$group.2)

LDA.scat.sorc <- ggplot(LD.data.source, aes(x = LD1, y = LD2))+
  geom_point(size = 4, color="black",
             aes(fill = group.2, color = group.2, shape = group.2), alpha = 0.9) +
  stat_ellipse(type = "norm", level = 0.95, linewidth = 0.5, aes(lty = group.2, color = group.2))+
  ylim(-4.5,5)+ xlim(-6,6)+
  scale_color_manual(values = lda.3.colors)+
  scale_fill_manual(values = lda.3.colors)+
  scale_shape_manual(values = c(24, 24, 24, 24))+
  scale_linetype_manual(values = c(2,2,2,2))+
  xlab("LD1 (82%)") + ylab("LD2 (18%)") +
  ggtitle(expression(paste("LDA: Measured-", delta^{13}, C[EAA]))) +
  theme_classic() + guides(lty = "none")

LDA.scat.sorc
dev.print(pdf, "figures/Fig.S6.LDA.scat.pdf", height=5.5, width=6.5)
dev.off()

```


### MixSIAR
LDA is great at classification, but cannot handle mixtures. Use a 3 member mixing model (reef plankton, pelagic plankton, coral) in MixSIAR to generate estimates of resource use. Once these models run, then perform *a posteriori* aggregation to generate estimates for 2 sources: inshore vs. offshore. 
  
First, load in sources and generate the means and SD for each normalized-EAA.
```{r MixSIAR loading and sources}
################################################
## MIXSIAR #####################################
################################################

# NORMALIZED DATA
# create a new data frame with desired columns for the consumers (mixtures)
Mix.df.EAAn <- raw.norm.LD.data %>%
  dplyr::select("SampleID_new", "Group", "Thrn", "Valn", "Leun", "Phen", "Lysn")

# remove sources
Mix.df.EAAn.consm <- Mix.df.EAAn[!(Mix.df.EAAn$Group == "ReefPlankton" | Mix.df.EAAn$Group == "PelagicPlankton" |
                                  Mix.df.EAAn$Group == "Coral"),]
Mix.df.EAAn.consm$Group <- droplevels(Mix.df.EAAn.consm$Group)
Mix.df.EAAn.consm<-na.omit(Mix.df.EAAn.consm)

# write and export a csv of the data frame
write.csv(Mix.df.EAAn.consm, "output/EAAn_Mix/Walak_Mix.df.EAAn.consumer.csv", row.names = FALSE)

###########
# SOURCES #
###########

# NORMALIZED DATA
# Reef and pelagic plankton, coral

# create a new data frame with desired columns for the primary producers (sources)
Source.df.EAA <-  Mix.df.EAAn[(Mix.df.EAAn$Group == "ReefPlankton" | Mix.df.EAAn$Group == "PelagicPlankton" |
                               Mix.df.EAAn$Group == "Coral"),]

Source.df.EAAn <- Source.df.EAA %>%
  dplyr::select("Group", "Thrn", "Valn", "Leun", "Phen", "Lysn")

##### source mean and SD
### Thr mean and SD
# calculate mean Thr values for the sources
mean.Thr.df.EAAn <- aggregate(Thrn ~ Group, data = Source.df.EAAn, FUN = mean)
colnames(mean.Thr.df.EAAn) <- c("Group", "Mean.Thrn")

# calculate standard deviations of Thr values for the sources
sd.Thr.df.EAAn <- aggregate(Thrn ~ Group, data = Source.df.EAAn, FUN = sd)
colnames(sd.Thr.df.EAAn) <- c("Group", "SD.Thrn")

### Val mean and SD
# calculate mean Val values for the sources
mean.Val.df.EAAn <- aggregate(Valn ~ Group, data = Source.df.EAAn, FUN = mean)
colnames(mean.Val.df.EAAn) <- c("Group", "Mean.Valn")

# calculate standard deviations of Val values for the sources
sd.Val.df.EAAn <- aggregate(Valn ~ Group, data = Source.df.EAAn, FUN = sd)
colnames(sd.Val.df.EAAn) <- c("Group", "SD.Valn")

### Leu mean and SD
# calculate mean Leu values for the sources
mean.Leu.df.EAAn <- aggregate(Leun ~ Group, data = Source.df.EAAn, FUN = mean)
colnames(mean.Leu.df.EAAn) <- c("Group", "Mean.Leun")

# calculate standard deviations of Leu values for the sources
sd.Leu.df.EAAn <- aggregate(Leun ~ Group, data = Source.df.EAAn, FUN = sd)
colnames(sd.Leu.df.EAAn) <- c("Group", "SD.Leun")

### Phe mean and SD
# calculate mean Phe values for the sources
mean.Phe.df.EAAn <- aggregate(Phen ~ Group, data = Source.df.EAAn, FUN = mean)
colnames(mean.Phe.df.EAAn) <- c("Group", "Mean.Phen")

# calculate standard deviations of Phe values for the sources
sd.Phe.df.EAAn <- aggregate(Phen ~ Group, data = Source.df.EAAn, FUN = sd)
colnames(sd.Phe.df.EAAn) <- c("Group", "SD.Phen")

### Lys mean and SD
# calculate mean Phe values for the sources
mean.Lys.df.EAAn <- aggregate(Lysn ~ Group, data = Source.df.EAAn, FUN = mean)
colnames(mean.Lys.df.EAAn) <- c("Group", "Mean.Lysn")

# calculate standard deviations of Phe values for the sources
sd.Lys.df.EAAn <- aggregate(Lysn ~ Group, data = Source.df.EAAn, FUN = sd)
colnames(sd.Lys.df.EAAn) <- c("Group", "SD.Lysn")

#### sample size
# calculate the number of samples for each of the sources
df.size.EAAn <- aggregate(Lysn ~ Group, data = Source.df.EAAn, FUN = length)
colnames(df.size.EAAn) <- c("Group", "Lys.size")

# bind the values calculated above into one data frame
source.agg.df.EAAn <- cbind(df.size.EAAn, 
                           mean.Thr.df.EAAn[2], sd.Thr.df.EAAn[2], 
                           mean.Val.df.EAAn[2], sd.Val.df.EAAn[2], 
                           mean.Leu.df.EAAn[2], sd.Leu.df.EAAn[2], 
                           mean.Phe.df.EAAn[2], sd.Phe.df.EAAn[2], 
                           mean.Lys.df.EAAn[2], sd.Lys.df.EAAn[2])
colnames(source.agg.df.EAAn) <- c("", "n", "MeanThrn", "SDThrn", "MeanValn", "SDValn", 
                                 "MeanLeun", "SDLeun", "MeanPhen", "SDPhen", "MeanLysn", "SDLysn")

# write and export a csv of the source data frame
write.csv(source.agg.df.EAAn, "output/EAAn_Mix/Walak_source.agg.df.EAAn.csv", row.names = FALSE, na = "")
```

Now, load in the consumers. We use a TDF of 0 (with SD of 0) assuming direct routing of EAAs to consumers.  
```{r load consumers}
# load consumer data and assign factors
# SampleID_new is a fixed factor
cons.EAAn <- load_mix_data(filename = "output/EAAn_Mix/Walak_Mix.df.EAAn.consumer.csv",
                          iso_names = c("Thrn", "Valn", "Leun", "Phen", "Lysn"),
                          factors = "SampleID_new",
                          fac_random = FALSE,
                          fac_nested = FALSE,
                          cont_effects = NULL)

# load source data
source.EAAn <- load_source_data(filename = "output/EAAn_Mix/Walak_source.agg.df.EAAn.csv",
                               source_factors = NULL,
                               conc_dep = FALSE,
                               data_type = "means",
                               cons.EAAn)

#Load TDF data
discr.EAAn <- load_discr_data(filename = "output/EAAn_Mix/MixSIAR_EAAn_TDFs.csv", cons.EAAn)
```

Set up here for the model (JAGs), run the model and save the output as an R.data file (can be memory intensive). For the sake of the Rmd-knitr, this is set to `eval=FALSE`.
```{r MixSIAR model Jags, cache=TRUE, results='hide', eval=FALSE}
################## ################## ################# 
################## RUN MIXSIAR MODEL BY SAMPLEID #######

# define the model and error structure and write JAGS model file
# PROCESS ERROR ONLY - HAVE TO DO THIS WHEN RUNNING MIXTURE POINTS INDIVIDUALLY

model_filename.EAAn <- "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_Model_SampleID_fixed.txt"
resid_err <- FALSE
process_err <- TRUE
write_JAGS_model(model_filename.EAAn, resid_err, process_err, cons.EAAn, source.EAAn)

################################################
############# run the JAGS model

set.seed(15)
consm.jags.mod.EAAn <- run_model(run = "long", cons.EAAn, source.EAAn, discr.EAAn, model_filename.EAAn, alpha.prior = 1, resid_err, process_err)

#save RDS object of JAGS model
saveRDS(consm.jags.mod.EAAn, file = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_Model_SampleID_rjags_norm.rds")
```

```{r MixSIAR model load in}
# code below will read back in the RDS object
consm.jags.mod.EAAn <- readRDS(file = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_Model_SampleID_rjags_norm.rds")
```

Generate plots for MixSIAR diagnostics. See the `MixSIAR model statistics` here. Gelman-Rubin values are < 1.05, and 2 of 3 chains in Geweke diagnostic perform well at <5% z-score.
```{r MixSIAR output options, message= 'hide', fig.show='hide', resuts='hide'}
# note the processing here opens files in quartz and results in an issue with knitting. This is why code chunk is set to "eval = FALSE". Code runs otherwise.

# process diagnostics, summary stats, and posterior plots
output_options_EAAn <- list(
  summary_save = TRUE,                   
  summary_name = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_summary_stats.EAAn",
  sup_post = FALSE,                       
  plot_post_save_pdf = FALSE,              
  plot_post_name = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_posterior_density.EAAn",   
  sup_pairs = TRUE,                       
  plot_pairs_save_pdf = FALSE,             
  plot_pairs_name = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_pairs_plot.EAAn",         
  sup_xy = FALSE,                          
  plot_xy_save_pdf = TRUE,                
  plot_xy_name = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_xy_plot.EAAn",             
  gelman = TRUE,                         
  heidel = FALSE,                         
  geweke = TRUE,                         
  diag_save = TRUE,                       
  diag_name = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_diagnostics.EAAn",
  indiv_effect = FALSE,
  plot_post_save_png = FALSE,            
  plot_pairs_save_png = FALSE,            
  plot_xy_save_png = FALSE,
  graphics.off())
```

```{r}
output_diagnostics(consm.jags.mod.EAAn, cons.EAAn, source.EAAn, output_options_EAAn)
```

Output for figures causes quartz device to open, which also gives issue with knit in generating the html output. For the sake of the Rmd-knitr, this is set to `eval=FALSE`.
```{r MixSIAR diagnostics table, message= 'hide', fig.show='hide', resuts='hide', eval=FALSE}
# set max to get all data
options(max.print = 3000)
out.JAGS <- output_JAGS(consm.jags.mod.EAAn, cons.EAAn, source.EAAn, output_options_EAAn)

# get the output stats for the model
# output_stats(consm.jags.mod.EAAn, cons.EAAn, source.EAAn, output_options_EAAn)
```

Perform source aggregation to "inshore vs. offshore".
```{r a posteriori aggregation, results='hide', message='hide'}
######### a posteriori aggregation of sources
# combine benthic algae and POM into one autochthonous source
consm.in.out.combined.EAAn <- combine_sources(consm.jags.mod.EAAn, cons.EAAn, source.EAAn, alpha.prior = 1,
                                          groups = list(Inshore = c("ReefPlankton", "Coral"),
                                                        Offshore = "PelagicPlankton"))

# print and save the interval plot
plot_intervals(consm.in.out.combined.EAAn, toplot = "fac1")
dev.copy(pdf, "output/MixSIAR_models/EAAn_SampleID_model/combines_output_intervals.EAAn.pdf", width=10, height=10)
dev.off()

#### # summary statistics for a posteriori aggregation of sources (inshore vs offshore)
# ***** NOTE ***** you will need to create a blank txt file if you run and re-run the aggregation. The file is not overwritten when you re-save in summary_stat(...). It will add outputs sequentially, which will make life terrible and code not-reproducible.

# make blank txt if re-running summary_stat(...)
file.create("output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_summary_stats_a_posteriori.EAAn.txt")

# run summary_stat and save output
summary_stat(consm.in.out.combined.EAAn, toprint = "fac1", meanSD = TRUE, savetxt = TRUE,
             quantiles = c(0.025, 0.05, 0.25, 0.5, 0.75, 0.95, 0.975),
             filename = "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_summary_stats_a_posteriori.EAAn")
```

```{r clean MixSIAR output}
################################################
### CLEAN MIXSIAR MODEL BY SAMPLEID RESULTS ###
################################################

SAMP.mixSIAR.EAAn.output.3 <- read.table(
  "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_summary_stats.EAAn.txt",
  sep = '\t', header = FALSE, skip = 6)

SAMP.mixSIAR.EAAn.output.3 <- as.data.frame(SAMP.mixSIAR.EAAn.output.3)
colnames(SAMP.mixSIAR.EAAn.output.3) <- "full.data"

# remove all extra spaces using "squish", make them 1 space only
library(stringr)
SAMP.mixSIAR.EAAn.output.3$full.data <- str_squish(SAMP.mixSIAR.EAAn.output.3$full.data)

# rename columns
library(plyr)
samp.mix.EAAn.out.3 <- separate(SAMP.mixSIAR.EAAn.output.3, full.data,  
                               into = c("factor", "Mean", "SD", "2.5.perc", "5.perc", "25.perc", "50.perc",
                                        "75.perc", "95.perc", "97.5.perc"), sep=" ")

# parse the factors here into 3 columns
new.cols.EAAn.3 <- stringr::str_split_fixed(samp.mix.EAAn.out.3$factor, "\\.", 3) %>%
  as.data.frame() %>%
  setNames(c("proportion", "SampleID_new", "Source"))

new.cols.EAAn.3 <- new.cols.EAAn.3 %>%
  dplyr::select("SampleID_new", "Source")

# combine the new, separated columns with the data, verify the levels match, then remove "factor"
Samp.out.cleaned.EAAn.3 <- cbind(new.cols.EAAn.3, samp.mix.EAAn.out.3[1:10])
Samp.out.cleaned.EAAn.3 <- Samp.out.cleaned.EAAn.3 %>%
  dplyr::select(-factor)

Samp.out.cleaned.EAAn.3<-Samp.out.cleaned.EAAn.3[-1,]

write.csv(Samp.out.cleaned.EAAn.3, "output/EAAn_Mix/Walak_MixSIAR_SampleID.df.EAAn.3sources.csv", row.names = FALSE)
```

#### 3 source MixSIAR plot 
Plot of three sources and their contribution to consumers. **This is Figure 6a**

```{r 3 sources MixSIAR, results='hide', fig.cap="**Figure 6a.** Results of the MixSIAR Bayesian mixing model using δ^13^C~EAA~ fingerprints. Proportional contribution of pelagic plankton, reef-associated plankton and corals to Giant trevally and Galapagos sharks.", fig.dim=c(6.5,6)}
################## ready to plot ################## 

# bring in some metadata to make sense of it
Mix.samp.EAAn.3 <- merge.data.frame(raw.norm.LD.data, Samp.out.cleaned.EAAn.3, by="SampleID_new")
Mix.samp.EAAn.3$Source <- factor(Mix.samp.EAAn.3$Source, 
                                 levels = c("PelagicPlankton", "ReefPlankton", "Coral"))
Mix.samp.EAAn.3$Mean<-as.numeric(Mix.samp.EAAn.3$Mean) 
Mix.samp.EAAn.3$SD<-as.numeric(Mix.samp.EAAn.3$SD) 

# plot it: some general formatting here for the plot...
Fig.formatting <- 
  theme(axis.ticks.length = unit(0.2, "cm"),
        axis.text.x = element_text(size = 12, color = "black"),
        axis.text.y = element_text(size = 12, color = "black"),
        axis.title.x = element_text(size = 14, color = "black"),
        axis.title.y = element_text(size = 14, color = "black"),
        legend.text = element_text(size = 10, color = "black"),
        legend.title = element_blank(),
        axis.ticks = element_line(color = "black"),
        legend.key.size = unit(0.6, "cm"))


## plot the 3 sources and consumers
Mix.samp.plot.EAAn.3 <- ggplot(data = Mix.samp.EAAn.3, aes(x = Group, y = Mean)) +
  geom_pointrange(aes(color=Source, x= Group:Source, ymin=Mean-SD, ymax=Mean+SD), 
                  position = position_jitter(width=0.2), alpha=0.5) +
  geom_boxplot(aes(color=Source, fill=Source, x= Group:Source), alpha = 0.55, size=0.6)+
  scale_fill_manual(values = c("lightblue3", "chartreuse4","gold3")) +
  scale_color_manual(values = c("lightblue4", "olivedrab4","goldenrod3"))+
  ylab("Proportional Contribution") +
  xlab(NULL) +
  ylim(0,1) +
  guides(fill = guide_legend(title = expression(paste(AA[ESS], " ", Source)))) +
  theme_classic() + Fig.formatting + guides(color = "none")

#print and save
Mix.samp.plot.EAAn.3
dev.copy(pdf, "figures/Fig.6A.Walak_Fig.mixsiar.3sour.EAAn.rev2.pdf", width = 10, height = 6)
dev.off()
```

```{r 2 sources MixSIAR cleanup, echo=FALSE}
################################ #################### ##########
############ a posteriori pooling: 2 sources ###################

### CLEAN MIXSIAR MODEL BY SAMPLEID RESULTS ###

SAMP.mixSIAR.EAAn.output <- read.table(
  "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_summary_stats_a_posteriori.EAAn.txt",
  sep = '\t', header = FALSE, skip = 6)

SAMP.mixSIAR.EAAn.output <- as.data.frame(SAMP.mixSIAR.EAAn.output)
colnames(SAMP.mixSIAR.EAAn.output) <- "full.data"

# remove all extra spaces using "squish", make them 1 space only
library(stringr)
SAMP.mixSIAR.EAAn.output$full.data <- str_squish(SAMP.mixSIAR.EAAn.output$full.data)

# rename columns
library(plyr)
samp.mix.EAAn.out <- separate(SAMP.mixSIAR.EAAn.output, full.data,  
                             into = c("factor", "Mean", "SD", "2.5.perc", "5.perc", "25.perc", "50.perc",
                                      "75.perc", "95.perc", "97.5.perc"), sep=" ")

# parse the factors here into 3 columns
new.cols.EAAn <- stringr::str_split_fixed(samp.mix.EAAn.out$factor, "\\.", 3) %>%
  as.data.frame() %>%
  setNames(c("proportion", "Source", "SampleID_new"))

new.cols.EAAn <- new.cols.EAAn %>%
  dplyr::select("SampleID_new", "Source")

# combine the new, separated columns with the data, verify the levels match, then remove "factor"
Samp.out.cleaned.EAAn <- cbind(new.cols.EAAn, samp.mix.EAAn.out[1:10])
Samp.out.cleaned.EAAn <- Samp.out.cleaned.EAAn %>%
  dplyr::select(-factor)

write.csv(Samp.out.cleaned.EAAn, "output/MixSIAR_models/EAAn_SampleID_model/MixSIAR_SampleID_a_posteriori.df.EAAn.csv", row.names = FALSE)
```

#### 2 member MixSIAR plot
Using aggregated sources, plot the % contribution of inshore vs. offshore food web support to consumers
```{r 2 sources MixSIAR plot, results='hide', fig.cap="**Figure. NA** Results of the MixSIAR Bayesian mixing model using δ^13^C~EAA~ fingerprints. Proportional contribution of inshore versus offshore resources to giant trevally and Galapagos sharks.", fig.dim=c(5,5)}
samp.mix.df<-Samp.out.cleaned.EAAn
samp.mix.reduced <- samp.mix.df %>% 
  dplyr::select(SampleID_new, Source, Mean, SD)

################# ################### ################ ################

# bring in some metadata to make sense of it
Mix.samp.EAAn.2 <- merge.data.frame(raw.norm.LD.data, samp.mix.reduced, by="SampleID_new")
Mix.samp.EAAn.2$Source <- factor(Mix.samp.EAAn.2$Source, 
                                 levels = c("Offshore", "Inshore"))
Mix.samp.EAAn.2$Mean<-as.numeric(Mix.samp.EAAn.2$Mean) 
Mix.samp.EAAn.2$SD<-as.numeric(Mix.samp.EAAn.2$SD) 

Mix.samp.EAAn.2.simp<- Mix.samp.EAAn.2 %>% select(SampleID_new, Group, LD.class, Source, Mean, SD)
write.csv(Mix.samp.EAAn.2.simp, "output/Walak_Mix.samp.EAAn.2sources_simple.csv")

########### plot
Mix.samp.plot.EAAn.InOff <- ggplot(data = Mix.samp.EAAn.2, aes(x = Group, y = Mean)) +
  geom_pointrange(aes(color=Source, x= Group:Source, ymin=Mean-SD, ymax=Mean+SD), 
                  position = position_jitter(width=0.2), alpha=0.5) +
  geom_boxplot(aes(color=Source, fill=Source, x= Group:Source), alpha = 0.5, size=0.6)+
  scale_fill_manual(values = c("lightblue3", "chartreuse4")) +
  scale_color_manual(values = c("lightblue4", "olivedrab4")) +
  ylab("Proportional Contribution") +
  xlab(NULL) +
  ylim(0,1) +
  guides(fill = guide_legend(title = expression(paste(AA[ESS], " ", Source)))) +
  theme_classic() + Fig.formatting + guides(color = "none")

#print and save grouped with 2 categories
Mix.samp.plot.EAAn.InOff
dev.copy(pdf, "figures/Fig.NA.Walak_Fig.mixsiar.2sour.EAAn.rev.pdf", width = 8, height = 5)
dev.off()
```

### Residency index
Here we use the residency index, or the events where the consumers were recorded by receiver arrays at French Frigate Shoals relative to the days the receivers were recording, to compare the proportion of inshore food web support to consumers.

We show that there is a significant correlation between the residency index and the % inshore food web use, dominated by the Giant trevlly, which are best represented in our dataset. The relationship is significant R~2~ = 0.55 and *p*<0.001.
```{r residency by inshore proportion EAA}
Inshore.df<-Mix.samp.EAAn.2[(Mix.samp.EAAn.2$Source=="Inshore"),]
Inshore.df$Group<-droplevels(Inshore.df$Group)
# write.csv(Inshore.df, "output/Inshore.df.csv")

# group mean model
Inshore.mod<-lm(Mean~Group, data=Inshore.df) #p=0.610, no difference in % inshore by group
Group.model<-anova(Inshore.mod)

# residency by mean model
Res.Inshore.mod<-lm(Mean~RI_corrected, data=Inshore.df)
Res.sum<-summary(Res.Inshore.mod) #R2-adj = 0.37,  #p=0.373
anova(Res.Inshore.mod)
```

This relationship is unchanged by the presence or absence of sharks in the data. Here, just giant trevally are used in the model, Adj-R~2~ = 0.56 and *p*=0.002.
```{r GT residency by inshore proportion EAA}
# residency model if GS removed
Res.Inshore.mod.GT<-lm(Mean~RI_corrected, data=Inshore.df[(Inshore.df$Group=="GT"),])
Res.sum<-summary(Res.Inshore.mod.GT) #R2-adj = 0.03, p=0.266
anova(Res.Inshore.mod.GT)
```

#### Residency by proportion of inshore foodweb support 
Plot the relationship for residency index for all sames (sharks and trevally). **This is Figure 6b**
```{r res by inshore proportion EAA, fig.cap="**Figure 6B.** Relationship between the proportion of inshore carbon sources utilized versus and the predator residency index. The correlation is significant (ANOVA, f = 20.943, p <0.001)", fig.dim=c(5,5)}
Inshore.prop<-ggplot(Inshore.df, aes(x=RI_post_3mo, y=Mean)) +
  geom_smooth(method = lm, color="seagreen", alpha=0.2, fill="mediumseagreen")+
  geom_point(color="seagreen", size=2.5, aes(shape=Group))+
  ylab("Proportion Inshore-EAA")+
  xlab("Residency Index")+
  annotate("text", x = 0.10, y = 0.68, label = "Adj-R2=0.55, p<0.001", size = 3) +
  theme_classic()

Inshore.prop
ggsave("figures/Fig.6B.Walak_Inshore.prop.EAAn.pdf", width=5, height=5)
```
